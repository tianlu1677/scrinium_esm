<% content_for :title, @experiment_spec.short_name %>

<% content_for :left do %>
  <%= render 'profile_left' %>
<% end %>

<% content_for :main do %>
  <%=
    render partial: 'layouts/navtab',
    locals: {
      tabs: {
        info: [
          t('global.info'),
          render('info')
        ],
        resources: [
          t('scrinium.resource'),
          render(partial: 'resources/table_list', locals: { app: scrinium_esm, resources: @experiment_spec.resources })
        ]
      }
    }
  %>
<% end %>
