<% content_for :title, t('activerecord.models.scrinium_esm/atm_model') %>
<% add_breadcrumb t('activerecord.models.scrinium_esm/atm_model'), atm_models_path, on_nav_bar: true %>

<% content_for :main_block do %>
  <%= render 'scrinium_esm/commons/model_list', models: ScriniumEsm::AtmModel.all %>
  <% if policy(ScriniumEsm::AtmModel).create? %>
    <%= link_to t('action.add'), new_atm_model_path, class: 'button float-right' %>
  <% end %>
<% end %>
