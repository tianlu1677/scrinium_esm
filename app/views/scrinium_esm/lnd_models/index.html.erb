<% content_for :title, t('scrinium_esm.lnd_model') %>
<% add_breadcrumb t('scrinium_esm.lnd_model'), lnd_models_path, on_nav_bar: true %>

<% content_for :main_block do %>
  <%= render 'scrinium_esm/commons/model_list', models: ScriniumEsm::LndModel.all %>
  <% if policy(ScriniumEsm::LndModel).create? %>
    <%= link_to t('action.add'), new_lnd_model_path, class: 'button float-right' %>
  <% end %>
<% end %>
