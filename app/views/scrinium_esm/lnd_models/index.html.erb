<% content_for :title, t('activerecord.models.scrinium_esm/lnd_model') %>
<% add_breadcrumb t('activerecord.models.scrinium_esm/lnd_model'), lnd_models_path, on_nav_bar: true %>

<% content_for :main_block do %>
  <%= render 'table_list' %>
  <% if policy(ScriniumEsm::LndModel).create? %>
    <%= link_to t('action.add'), new_lnd_model_path, class: 'button float-right' %>
  <% end %>
<% end %>
