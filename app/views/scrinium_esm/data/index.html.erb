<% content_for :title, t('scrinium_esm.data') %>
<% add_breadcrumb t('scrinium_esm.data'), scrinium_esm.data_path, on_nav_bar: true %>

<% content_for :main_block do %>
  <%=
    render partial: 'scrinium_esm/data/table_list',
    locals: { data: ScriniumEsm::Datum.all }
  %>
  <% if policy(ScriniumEsm::Datum).create? %>
    <%= link_to t('action.add'), scrinium_esm.new_datum_path, class: 'button float-right' %>
  <% end %>
<% end %>
